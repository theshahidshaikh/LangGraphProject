# Agentic Excel Task Executor (LangGraph + Gemini)

This project runs an agent that reads tasks from an Excel file, understands the intent using an LLM, executes the task, and writes results back to Excel.

---

## 🔑 Add Your API Key

This project uses **Google Gemini**.

So set them up 


